series: xenial
services:
  ghost:
    charm: "cs:ghost-19"
    num_units: 1
    options:
      config: include-file://config1.yaml
  mysql:
    charm: "cs:trusty/mysql-57"
    num_units: 1
  test:
    charm: "../charm"
relations:
  - ["ghost", "mysql"]
